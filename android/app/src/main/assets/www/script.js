import * as THREE from './three.module.js';
const CHAPTERS=[{title:'Echoes Beneath the Reeds',summary:'Blue lanterns have appeared across Moonreed Valley. Find the drowned bell.',objectives:['Speak with the Lantern Keeper','Defeat the Mist Wraith','Reach the Old Moon Shrine']},{title:'The Bell That Sank',summary:'An ancient bell lies beneath the shrine.',objectives:['Enter the shrine','Solve the moon seal','Defeat the Bell Warden']},{title:'Threads of Jade',summary:'The bell reveals a hidden road toward Yunjing.',objectives:['Cross Mistwood','Meet Ren Yue','Uncover the jade sigil']}];
const ENEMIES=[{id:'wraith',name:'Mist Wraith',hp:100,xp:35,scale:1,color:0x416e66},{id:'bandit',name:'Reed Bandit',hp:150,xp:55,scale:1.1,color:0x6d4035},{id:'fox',name:'Moonfang Fox',hp:75,xp:40,scale:.8,color:0x9b7652},{id:'warden',name:'Bell Warden',hp:420,xp:180,scale:1.7,color:0x78664c}];
const NPCS=[{id:'keeper',name:'Lantern Keeper',x:-17,z:-4,text:'The valley bell sank three nights ago. Since then, the dead have begun walking.'},{id:'ren',name:'Ren Yue',x:27,z:-25,text:'The jade sigil is not a treasure. It is a promise made by someone who never returned.'},{id:'merchant',name:'Old Bo',x:-12,z:-16,text:'Weapons, herbs and charms. Silver buys almost anything except a clean conscience.'}];
const $=id=>document.getElementById(id),game=$('game');const state={hp:100,maxHp:100,qi:100,maxQi:100,xp:0,level:1,silver:240,quest:0,objective:0,damage:28,herb:2,mooncut:false,ironwill:false,keys:{},attacking:false};let scene,camera,renderer,clock,player,worldGroup,enemyGroup,npcGroup,spawned=[];const moveDir=new THREE.Vector3();const mat=(c,r=.8)=>new THREE.MeshStandardMaterial({color:c,roughness:r});const box=(w,h,d,c)=>new THREE.Mesh(new THREE.BoxGeometry(w,h,d),mat(c));const cyl=(r,h,c)=>new THREE.Mesh(new THREE.CylinderGeometry(r,r*.82,h,10),mat(c));
function tree(x,z,s=1){let g=new THREE.Group(),t=cyl(.24*s,1.5*s,0x493126);t.position.y=.75*s;g.add(t);for(let i=0;i<3;i++){let c=new THREE.Mesh(new THREE.ConeGeometry((1.3-i*.2)*s,2.1*s,8),mat(i===2?0x16382f:0x1d5140));c.position.y=(2+i*.95)*s;g.add(c)}g.position.set(x,0,z);worldGroup.add(g)}function house(x,z,s=1){let g=new THREE.Group(),b=box(3.8*s,2.4*s,3*s,0x76553d);b.position.y=1.2*s;g.add(b);let r=new THREE.Mesh(new THREE.ConeGeometry(2.8*s,1.5*s,4),mat(0x33292a));r.rotation.y=Math.PI/4;r.position.y=3*s;g.add(r);g.position.set(x,0,z);worldGroup.add(g)}
function modelCharacter(color,scale=1){let g=new THREE.Group(),body=cyl(.5*scale,1.6*scale,color);body.position.y=.95*scale;g.add(body);let head=new THREE.Mesh(new THREE.SphereGeometry(.4*scale,12,8),mat(0xd0a27f));head.position.y=2*scale;g.add(head);let hair=cyl(.43*scale,.3*scale,0x171d1d);hair.position.y=2.3*scale;g.add(hair);g.traverse(o=>o.castShadow=true);return g}
function init(){scene=new THREE.Scene();scene.background=new THREE.Color(0x102f2d);scene.fog=new THREE.Fog(0x102f2d,35,125);camera=new THREE.PerspectiveCamera(58,innerWidth/innerHeight,.1,220);renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;game.innerHTML='';game.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xb8dfd5,0x17251f,2));let sun=new THREE.DirectionalLight(0xd9e8df,2.8);sun.position.set(-30,45,20);sun.castShadow=true;sun.shadow.mapSize.set(1024,1024);scene.add(sun);worldGroup=new THREE.Group();enemyGroup=new THREE.Group();npcGroup=new THREE.Group();scene.add(worldGroup,enemyGroup,npcGroup);buildWorld();player=modelCharacter(0x294d49);player.position.set(0,0,20);let sword=box(.12,2.3,.16,0xd8d6c7);sword.position.set(.75,1.2,0);sword.rotation.z=-.35;player.add(sword);scene.add(player);spawnNPCs();spawnEnemies();clock=new THREE.Clock();updateHud();setTimeout(()=>{$('loading').classList.add('gone');toast('Chapter I · Echoes Beneath the Reeds')},900);animate()}
function buildWorld(){let ground=box(110,.5,110,0x173c31);ground.position.y=-.3;ground.receiveShadow=true;worldGroup.add(ground);let river=box(9,.08,105,0x286d68);river.position.set(13,.02,0);river.rotation.y=-.12;worldGroup.add(river);let path=box(5,.1,100,0x746247);path.position.set(-2,.03,0);path.rotation.y=.18;worldGroup.add(path);for(let i=0;i<95;i++){let a=Math.random()*Math.PI*2,r=30+Math.random()*34;tree(Math.cos(a)*r,Math.sin(a)*r,.65+Math.random()*.65)}for(let i=0;i<8;i++)house(-22+i*4.5,-11+(i%2)*4,.75)}
function spawnNPCs(){for(const n of NPCS){let g=modelCharacter(n.id==='merchant'?0x7a6746:n.id==='ren'?0x5d3d65:0x8c6b42,.9);g.position.set(n.x,0,n.z);g.userData=n;npcGroup.add(g)}}function spawnEnemies(){const spots=[['wraith',4,8],['bandit',-15,-24],['bandit',22,5],['fox',-29,20],['fox',32,25],['warden',27,-35]];for(const [id,x,z] of spots){let d=ENEMIES.find(e=>e.id===id),g=modelCharacter(d.color,d.scale);g.position.set(x,0,z);g.userData={...d,baseHp:d.hp};enemyGroup.add(g);spawned.push(g)}}
function updateHud(){$('hpBar').style.width=state.hp/state.maxHp*100+'%';$('qiBar').style.width=state.qi/state.maxQi*100+'%';$('xp').textContent=state.xp;$('level').textContent=state.level;$('questTitle').textContent=CHAPTERS[state.quest].title;$('questText').textContent=CHAPTERS[state.quest].objectives[state.objective]||CHAPTERS[state.quest].summary}function nearestEnemy(){let b=null,bd=6;for(const e of spawned)if(e.visible){let d=player.position.distanceTo(e.position);if(d<bd){b=e;bd=d}}return b}
function attack(){if(state.attacking)return;let e=nearestEnemy();if(!e){toast('No enemy within striking distance');return}state.attacking=true;setTimeout(()=>{let dmg=state.damage+(state.mooncut?10:0);e.userData.hp-=dmg;toast(`${e.userData.name} · -${dmg} HP`);if(e.userData.hp<=0){e.visible=false;state.xp+=e.userData.xp;state.silver+=Math.round(e.userData.xp/2);state.objective=Math.min(2,state.objective+1);if(state.xp>=100){state.level++;state.xp-=100;state.maxHp+=12;state.maxQi+=8;state.damage+=4;toast('Level up!')}updateHud()}state.attacking=false},240)}function dash(){if(state.qi<18){toast('Not enough Qi');return}state.qi-=18;moveDir.set(0,0,-1).applyQuaternion(player.quaternion);player.position.addScaledVector(moveDir,6);updateHud();toast('Cloudstep')}function heal(){if(!state.herb){toast('No Moonleaf Herbs');return}state.herb--;state.hp=Math.min(state.maxHp,state.hp+25);updateHud();toast('Moonleaf restores 25 HP')}function interact(){let e=nearestEnemy();if(e){dialogue(e.userData.name,'The creature watches you as if it recognises the jade mark on your hand.',['Attack','Back away']);return}let a={n:null,d:7};for(const n of npcGroup.children){let d=player.position.distanceTo(n.position);if(d<a.d)a={n,d}}if(a.n){dialogue(a.n.userData.name,a.n.userData.text,['Tell me more','Goodbye']);return}toast('Nothing nearby to interact with')}
function dialogue(s,t,choices){$('speaker').textContent=s;$('dialogueText').textContent=t;$('choices').innerHTML=choices.map(c=>`<button>${c}</button>`).join('');$('dialogue').classList.remove('hidden');document.querySelectorAll('#choices button').forEach((b,i)=>b.onclick=()=>{$('dialogue').classList.add('hidden');if(i===0&&s==='Mist Wraith')attack();else toast('Your choice has consequences')})}function toast(t){$('toast').textContent=t;$('toast').classList.add('show');clearTimeout(window.tt);window.tt=setTimeout(()=>$('toast').classList.remove('show'),1800)}
function move(dt){moveDir.set(0,0,0);if(state.keys.w||state.keys.ArrowUp)moveDir.z-=1;if(state.keys.s||state.keys.ArrowDown)moveDir.z+=1;if(state.keys.a||state.keys.ArrowLeft)moveDir.x-=1;if(state.keys.d||state.keys.ArrowRight)moveDir.x+=1;if(moveDir.lengthSq()){moveDir.normalize();player.position.addScaledVector(moveDir,dt*9);player.rotation.y=Math.atan2(moveDir.x,moveDir.z);state.qi=Math.min(state.maxQi,state.qi+dt*4)}player.position.x=Math.max(-48,Math.min(48,player.position.x));player.position.z=Math.max(-48,Math.min(48,player.position.z));camera.position.lerp(new THREE.Vector3(player.position.x,8,player.position.z+11),.08);camera.lookAt(player.position.x,1.3,player.position.z);updateHud()}function animate(){requestAnimationFrame(animate);let dt=Math.min(clock.getDelta(),.05);move(dt);for(const e of spawned)if(e.visible){e.position.y=Math.sin(performance.now()*.002+e.position.x)*.08;if(e.userData.id==='fox')e.rotation.y+=dt*.7}renderer.render(scene,camera)}
let joy=false,lx=0,ly=0;$('attack').onclick=attack;$('dash').onclick=dash;$('interact').onclick=interact;$('heal').onclick=heal;$('skill').onclick=()=>{state.mooncut=true;toast('Mooncut learned')};$('menuBtn').onclick=()=>$('menu').classList.remove('hidden');$('closeMenu').onclick=()=>$('menu').classList.add('hidden');$('resume').onclick=()=>$('menu').classList.add('hidden');$('joystick').addEventListener('pointerdown',e=>{joy=true;lx=e.clientX;ly=e.clientY;e.currentTarget.setPointerCapture(e.pointerId)});$('joystick').addEventListener('pointermove',e=>{if(!joy)return;let dx=e.clientX-lx,dy=e.clientY-ly;if(Math.abs(dx)+Math.abs(dy)>4){state.keys.a=dx<0;state.keys.d=dx>0;state.keys.w=dy<0;state.keys.s=dy>0;lx=e.clientX;ly=e.clientY}});['pointerup','pointercancel'].forEach(v=>$('joystick').addEventListener(v,()=>{joy=false;state.keys.a=state.keys.d=state.keys.w=state.keys.s=false}));window.addEventListener('keydown',e=>{state.keys[e.key]=true;if(e.key===' ')attack();if(e.key.toLowerCase()==='e')interact();if(e.key.toLowerCase()==='shift')dash();if(e.key.toLowerCase()==='h')heal()});window.addEventListener('keyup',e=>state.keys[e.key]=false);window.addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});init();